# ============================================================
# audiax-ai — requirements.txt (instalasi dev lengkap, satu file)
# ============================================================
# pip install -r requirements.txt
#
# File ini mengonsolidasikan SEMUA dependensi repo (ai/ + service/ +
# experiments/ + testing + notebook) supaya sesi kerja baru (termasuk Claude
# Code) bisa `pip install -r requirements.txt` sekali dan langsung bisa:
# menjalankan pipeline AI, menjalankan service FastAPI, menjalankan notebook
# eksperimen, dan menjalankan test suite.
#
# `experiments/audiax_pipeline.ipynb` TIDAK memasang paket sendiri (tidak ada
# `%pip install` di dalamnya) -- semua dependensinya, termasuk PyTorch build
# CUDA di bawah, wajib sudah terpasang lewat file ini SEBELUM notebook dibuka.
#
# CATATAN untuk image Docker PRODUKSI: Dockerfile TIDAK memakai file ini.
# Ia memasang dari ai/requirements.txt + service/requirements.txt secara
# terpisah (lihat komentar di Dockerfile) supaya image tetap ramping dan
# supaya batas "ai/ nol dependensi web/non-esensial" (ditegakkan otomatis
# oleh tests/test_boundary.py) tetap terlihat jelas di level file, bukan
# cuma di level kode. Kalau menambah dependensi baru, tambahkan juga ke
# file spesifik yang relevan (ai/requirements.txt / service/requirements.txt
# / experiments/requirements.txt), bukan cuma di sini.

# --- PyTorch dengan dukungan CUDA 12.6 (wheel cu126 tersedia untuk Windows & Linux, Python 3.11) ---
# extra-index-url ini HANYA menambah sumber tambahan untuk torch/torchaudio --
# paket lain tetap resolve dari PyPI seperti biasa. Kalau environment tidak
# punya GPU NVIDIA, pip tetap memasang build ini (CUDA opsional saat runtime,
# torch.cuda.is_available() otomatis False) -- tidak perlu file terpisah untuk CPU-only.
--extra-index-url https://download.pytorch.org/whl/cu126

# --- Inti AI (ai/) — lihat ai/requirements.txt ---
torch==2.6.0+cu126
torchaudio==2.6.0+cu126
numpy==2.1.3
scipy==1.14.1

# --- Lapisan HTTP (service/) — lihat service/requirements.txt ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Evaluasi & eksperimen (experiments/) — lihat experiments/requirements.txt ---
pandas==2.2.3
scikit-learn==1.5.2
matplotlib==3.9.2
tabulate==0.9.0            # dipakai pandas.DataFrame.to_markdown() di notebook eksperimen
soundfile==0.13.1          # baca audio di notebook -- lihat CLAUDE.md §Stack (menghindari backend torchcodec)

# --- Testing (tests/) ---
pytest==8.3.4
httpx==0.28.1               # dipakai fastapi.testclient.TestClient

# --- Unduhan checkpoint (dipakai sel unduhan di kedua notebook) ---
onedrivedownloader==1.1.3

# --- Notebook (menjalankan/mengedit .ipynb secara lokal, di luar Colab) ---
jupyter==1.1.1
notebook==7.3.2
nbformat==5.10.4
ipykernel==6.29.5
