# ============================================================
# ai/ -- logika inti AUDIAX. NOL dependensi web (ditegakkan
# tests/test_boundary.py). Dipakai stage builder di Dockerfile.
# ============================================================
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.6.0+cpu
torchaudio==2.6.0+cpu      # resample; juga cadangan pembacaan audio
numpy==2.1.3
scipy==1.14.1                # filter Butterworth, STFT/ISTFT, median filter (HPSS), kurtosis
soundfile==0.13.1            # pembaca audio utama (lebih tahan daripada backend torchcodec)
